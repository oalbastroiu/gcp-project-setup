import {
  to = module.project-cfg.google_service_account.service_accounts["${SA_SHORT_NAME}"]
  id = "projects/${PROJECT}/serviceAccounts/${SA_FULL_NAME}"
}

import {
  to = module.tf-state-bucket.google_storage_bucket.bucket
  id = "${PROJECT}/${GCS_BUCKET}"
}
